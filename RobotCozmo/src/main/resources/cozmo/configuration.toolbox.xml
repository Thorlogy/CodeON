<!-- Cozmo's components are built in. The tab shows a read-only overview, so
     users must not add, remove or reconnect hardware blocks. -->
<toolbox_set id="toolbox-Configuration" style="display: none"/>
